# Create executables/builds/artifacts for C & C++ code
name: Create executables for C & C++ code

on:
  push:
    branches: [ "main", "develop", "stagging" ]
  pull_request:
    branches: [ "main", "develop", "stagging" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install build tools
      run: sudo apt-get update && sudo apt-get install -y build-essential
      
    - name: make
      run: make
      
    - name: make check
      run: |
        echo "Below are the list of files in $(pwd)"
        ls -lrt
        # scp cal.exe <IP>:/home/ubuntu/
        # ssh <IP> cd /home/ubuntu
        # ./cal.exe
        
    # - name: Upload a Build Artifact
    #   uses: actions/upload-artifact@v4.6.2
    #   with:
    #     # Artifact name
    #     name: cal.exe
    #     # A file, directory or wildcard pattern that describes what to upload
    #     path: $(pwd)
    #     # The desired behavior if no files are found using the provided path.
   
    
        include-hidden-files: # optional, default is false



